services:
  postgres:
    environment:
      POSTGRES_PASSWORD: ${SONAR_POSTGRES_PASSWORD}
    volumes:
      - /home/data/runtime/sonarqube/postgres:/var/lib/postgresql/data
  sonarqube:
    environment:
      SONAR_JDBC_PASSWORD: ${SONAR_POSTGRES_PASSWORD}
    volumes:
      - /home/data/runtime/sonarqube/data:/opt/sonarqube/data
      - /home/data/runtime/sonarqube/logs:/opt/sonarqube/logs
      - /home/data/runtime/sonarqube/extensions:/opt/sonarqube/extensions
